image: php:8.1-cli

stages:
  - install_dependencies
  - test
  - deploy

# 1. INSTALACIÓN
install_deps:
  stage: install_dependencies
  script:
    - echo "Preparando el entorno para KitchenLink..."

# 2. ETAPA DE TEST (4 trabajos en paralelo)

lint-job:
  stage: test
  needs: ["install_deps"]
  script:
    - find . -name "*.php" -exec php -l {} \; | grep "Errors parsing" && exit 1 || echo "✅ Sintaxis OK."

unit-test-job:
  stage: test
  needs: ["install_deps"]
  script:
    - php -r "if (file_exists('src/php/db_connection.php')) { echo 'Test DB PASSED'; } else { echo 'Test DB FAILED'; exit(1); }"
    - |
      echo '<?xml version="1.0" encoding="UTF-8"?>
      <testsuites>
        <testsuite name="UnitTests" tests="1" failures="0">
          <testcase name="DatabaseConnectionFile"/>
        </testsuite>
      </testsuites>' > junit.xml
  artifacts:
    when: always
    reports:
      junit: junit.xml

security-job:
  stage: test
  needs: ["install_deps"]
  script:
    - echo "Buscando archivos sensibles expuestos..."
    # Usamos un bloque condicional limpio para evitar errores de YAML
    - |
      if find . -maxdepth 1 -name "*.env" -o -name "*.sql" | grep -q .; then
        echo "❌ ERROR: Archivos sensibles (.env o .sql) detectados en la raíz!"
        exit 1
      else
        echo "✅ No se encontraron archivos sensibles."
      fi

phpunit-advanced-job:
  stage: test
  needs: ["install_deps"]
  script:
    - apt-get update && apt-get install -y git unzip zip libmariadb-dev
    - docker-php-ext-install mysqli
    - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
    - cd KitchenLink-Tests
    - composer install --no-interaction
    - ./vendor/bin/phpunit --testdox --log-junit report-phpunit.xml
  artifacts:
    when: always
    reports:
      junit: KitchenLink-Tests/report-phpunit.xml

# 3. DESPLIEGUE A GOOGLE CLOUD (Solo Main)
deploy_to_server:
  stage: deploy
  needs: ["lint-job", "unit-test-job", "security-job", "phpunit-advanced-job"]
  before_script:
    - apt-get update -y && apt-get install openssh-client rsync -y
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan $VM_IP_ADDRESS >> ~/.ssh/known_hosts
  script:
    - echo "🚀 Desplegando en Google Cloud vía Rsync..."
    # Excluimos archivos de desarrollo y backups para mayor seguridad
    - rsync -atv --delete --exclude='.git*' --exclude='.gitlab-ci.yml' --exclude='KitchenLink-Tests/' --exclude='*.sql' ./ $VM_USER@$VM_IP_ADDRESS:/var/www/html/
    - echo "✅ Despliegue completado con éxito."
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'