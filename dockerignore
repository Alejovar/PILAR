# Control de versiones
.git
.gitignore

# CI/CD
.gitlab-ci.yml
pipeline_adellya.txt

# Tests (no van en la imagen de producción)
KitchenLink-Tests/

# Archivos de configuración local
.env
.env.example

# Llaves SSH (NUNCA deben ir en la imagen)
id_rsa_kitchen
id_rsa_kitchen.pub

# Documentación
*.md
documentation/

# La carpeta docker en sí no va dentro del contenedor
# (nginx.conf y start.sh se copian explícitamente en el Dockerfile)
docker/
